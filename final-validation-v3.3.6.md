# HandFont Studio v3.3.6 최종 검증

## 변경 목적

로그인 없이 운영하는 웹서비스에서 모든 프로젝트가 공용 `anonymous` 소유자로 저장되던 문제를 수정했습니다. v3.3.6부터 브라우저마다 임의 토큰을 발급하고, 토큰의 SHA-256 해시를 프로젝트 `owner_id`로 사용합니다.

## 적용 범위

- 프로젝트 목록과 생성
- 프로젝트 상세·글리프 검수
- 프로젝트 삭제
- 로컬 파일 및 Vercel Blob 업로드
- 분석·폰트 생성 작업 등록과 상태 조회
- SVG·메타데이터·완성 폰트 다운로드
- Vercel Blob 업로드 완료 콜백

워커 내부 API는 기존 `WORKER_SHARED_SECRET` 인증을 유지합니다.

## 보안 동작

- 첫 GET 요청에서 256비트 브라우저 토큰 발급
- `HttpOnly`, `SameSite=Lax`, 운영 HTTPS에서 `Secure` 적용
- DB에는 토큰 원문이 아닌 SHA-256 해시 저장
- 소유자가 다른 프로젝트와 파일은 404 처리
- Vercel Blob 콜백에는 서버가 검증한 ownerId만 token payload로 전달
- 기존 `owner_id='anonymous'` 프로젝트는 자동 이전하지 않음

## 검증 결과

| 검사 | 결과 |
|---|---:|
| TypeScript 엄격 구문·계약 검사 (`tsconfig.syntax.json`) | 통과 |
| 웹 계약 테스트 | 11/11 통과 |
| Python 워커 테스트 | 4/4 통과 |
| Python compileall | 통과 |
| Node.js 문법 검사 | 통과 |
| 개인정보 사전 검사 | 337개 파일 통과 |
| 저장소 사전 검사 | 337개 파일 통과 |
| v3.3.4 기준 핫픽스 덮어쓰기 시뮬레이션 | 통과 |
| 전체·핫픽스 ZIP 무결성 | 통과 |
| 사용자 업로드 원본·파생 파일 포함 | 0개 |
| 폰트·DB 런타임 바이너리 포함 | 0개 |

## 미실행 항목

npm 레지스트리 DNS에 접근할 수 없어 `pnpm install`, 실제 패키지 타입을 사용하는 `pnpm typecheck`, Next.js Production Build는 이 환경에서 실행하지 못했습니다. 배포 후 Vercel의 새 Production Build가 최종 런타임 검증입니다.

## 배포 영향

- 새 환경변수는 필요하지 않습니다.
- 기존 데이터베이스 구조에서도 새 프로젝트는 정상 생성됩니다.
- `0002_anonymous_ownership.sql`은 owner 조회 인덱스와 기본값 제거를 위한 권장 마이그레이션이며 즉시 실행하지 않아도 기능은 동작합니다.
- v3.3.5 이하에서 생성한 공용 익명 프로젝트는 새 목록에 표시되지 않습니다.
- 브라우저 쿠키 또는 사이트 데이터를 지우면 익명 프로젝트 접근을 복구할 수 없습니다.

# HandFont Studio v3.3.6 Context Notes

## 목표
- 로그인 없이도 브라우저마다 독립된 익명 소유권을 발급한다.
- 프로젝트 목록, 상세, 업로드, 작업 실행, 결과 다운로드와 삭제를 소유자 범위로 제한한다.
- 공개 URL이나 프로젝트 ID만 알아도 다른 브라우저의 프로젝트를 열 수 없게 한다.

## 구현 범위
- `apps/web/proxy.ts`: 첫 방문 시 고엔트로피 익명 소유자 쿠키 발급
- `apps/web/lib/owner.ts`: 쿠키 검증과 SHA-256 소유자 ID 파생
- `apps/web/lib/repository.ts`: 사용자용 조회·수정 함수를 `owner_id`로 제한
- 공개 페이지와 API: 프로젝트·작업·업로드·Blob 접근 전에 소유권 확인
- 업로드 완료 콜백: 서버가 검증한 소유자 ID를 서명된 Blob token payload에 보존
- `infrastructure/migrations/0002_anonymous_ownership.sql`: 소유자 인덱스와 기본값 정리

## 소유권 정책
- 브라우저 쿠키에는 256비트 임의 토큰을 저장한다.
- 데이터베이스에는 원문 토큰이 아니라 SHA-256 해시만 `owner_id`로 저장한다.
- 쿠키는 `HttpOnly`, `Secure`(운영), `SameSite=Lax`, 1년 만료로 설정한다.
- 소유자가 다른 프로젝트는 존재 여부를 노출하지 않고 404로 응답한다.
- 워커 내부 API는 기존 `WORKER_SHARED_SECRET` 인증을 유지하며 소유자 쿠키를 요구하지 않는다.

## 기존 데이터
- v3.3.5 이하에서 생성된 `owner_id='anonymous'` 프로젝트는 보안상 자동 이전하지 않는다.
- 새 버전 배포 후 생성하는 프로젝트부터 브라우저별로 분리된다.
- 기존 테스트 프로젝트가 필요하지 않다면 Neon에서 별도로 정리할 수 있다.

## 운영 경계
- 브라우저 데이터나 쿠키를 삭제하면 해당 브라우저의 프로젝트에 다시 접근할 수 없다.
- 다른 기기 간 동기화와 복구는 향후 선택형 이메일 로그인에서 제공한다.
- Queue/API 메시지 `schemaVersion`은 기존 워커 호환성을 위해 `3.3.0`을 유지한다.

import {listGlyphs,getProject} from '@/lib/repository';import {notFound} from 'next/navigation';
export const dynamic='force-dynamic';
export default async function Review({params,searchParams}:{params:Promise<{projectId:string}>,searchParams:Promise<{status?:string,page?:string,q?:string}>}){const {projectId}=await params;const query=await searchParams;if(!await getProject(projectId))notFound();const glyphs:any[]=await listGlyphs(projectId,{status:query.status,page:query.page?Number(query.page):undefined,q:query.q}) as any[];return <main className="page"><h1>글리프 검수</h1><p className="muted">{glyphs.length}개 글리프</p><div className="glyphGrid">{glyphs.map(g=><article className="glyph" key={g.id}><div className="glyphPreview"><img src={`/api/blob?projectId=${projectId}&url=${encodeURIComponent(g.svg_url)}`} alt={`${g.character} 글리프`}/></div><div className="glyphMeta"><strong>{g.character}</strong><span>{g.cell_id}</span></div></article>)}</div></main>}

import postgres from 'postgres';import {readFile,readdir} from 'node:fs/promises';import path from 'node:path';import process from 'node:process';
const url=process.env.DATABASE_URL;if(!url)throw new Error('DATABASE_URL is required');const sql=postgres(url,{ssl:url.includes('localhost')?false:'require',max:1});const dir=path.resolve(process.cwd(),'../../infrastructure/migrations');for(const name of (await readdir(dir)).filter(n=>n.endsWith('.sql')).sort()){const source=await readFile(path.join(dir,name),'utf8');console.log('migrate',name);await sql.unsafe(source)}await sql.end();
